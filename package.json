{  
  "name": "workout-tracker-backend",
  "version": "0.0.0",
  "description": "",
  "engines": {
    "node": ">=16.0.0"
  },
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/index.ts --outfile=main.js --bundle --platform=node --packages=external --target=node16.0 --loader:.ts=ts",
    "dev": "nodemon --watch src --ext ts --exec run-s build start",
    "start": "node main.js"
  },
  "dependencies":{
    "express":"^4.18.2"
  },
  "devDependencies": {
    "@types/express":"^4.17.17",
    "esbuild": "^0.18.4",
    "nodemon": "^2.0.22",
    "npm-run-all":"^4.1.5",
    "typescript": "^5.1.3"
  }
}
